# Copy to compose/.env on the host. Selects both the Phase 2 runtime and the transitional
# shared agent server, so `docker compose up -d` in this directory needs no extra flags.
# Drop the second file once the gateway launches per-investigation containers.
COMPOSE_FILE=docker-compose.yml:docker-compose.shared-agent.yml
